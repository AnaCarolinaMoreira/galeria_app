import 'package:flutter/material.dart';

class GaleriaScreens extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Galeria"),),
      body: Center(child: Text("galeria"),),
    );
  }
}
